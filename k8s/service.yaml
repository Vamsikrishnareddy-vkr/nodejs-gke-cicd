apiVersion: v1
kind: Service
metadata:
  name: nodejs-gke-cicd-service
spec:
  type: NodePort
  selector:
    app: nodejs-gke-cicd
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30081